import { Config } from '../types';
export declare function generateTypes({ types, config }: {
    types: Record<string, any>;
    config: Config;
}): void;
export declare function generatingActionsTypes({ methods, types, config, }: {
    methods: Record<string, any>;
    types: Record<string, any>;
    config: Config;
}): void;
